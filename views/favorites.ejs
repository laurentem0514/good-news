<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <title></title>
  <link rel="stylesheet" href="">
</head>
<body>
  <h1>favorites</h1>
  <div class="favorites">
   <h3><%# favorites.length %> Favorites</h3>
        <ul class="favorites">
          <% for (let i = 0; i < favorites.length; i++) { %>
            <li class="">
              <img src="<%= favorites[i].logo %>" alt=""/>
              <h4><%= favorites[i].name %></h4>
            </li>
          <% }; %>
        </ul>
</div>
</body>
</html>
